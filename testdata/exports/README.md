# Placeholder — frozen ApiService JSON per rhcl_seed_* product (#280).
# Files: {system_name}.json (e.g. rhcl_seed_cors.json)
# Generate: [`scripts/refresh-seed-exports.sh`](../../scripts/refresh-seed-exports.sh) (manual, lab credentials)
