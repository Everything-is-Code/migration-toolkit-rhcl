version: 1
# P1 manifest: version + optional cluster_profile overrides (#288).
# Product index is convention-based (catalog.yaml → expectations.yaml → exports/*.json);
# per-product policies and output files are not duplicated here (see design.md).
profiles: {}
